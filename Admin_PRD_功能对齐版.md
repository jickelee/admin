# Anole Loan 巴基斯坦借贷业务后台 PRD（功能对齐版）

**版本：** V2.0  
**更新日期：** 2026-07-24  
**产品：** Anole Loan 内部运营后台  
**对齐基线：** 当前后台交互原型 `index.html`、业务 SQL `zariya_loan.sql`、用户与借款数据说明、Munsalik 一期页面方案  
**文档状态：** 当前功能基线，可用于产品评审、接口设计与验收拆分

> 本文描述当前后台原型已经呈现的页面、字段和操作。原型中的操作目前为前端模拟交互，不代表后端接口、数据持久化、权限拦截和审计链路已经完成。未在当前界面提供的能力统一列入“待实现”，不再写成现有功能。

---

## 1. 建设目标与范围

后台用于支撑巴基斯坦现金借贷业务的客户查询、授信审核、借款管理、账单与还款查询、逾期及催收处理、财务对账、优惠券运营、业务配置和系统权限管理。

当前原型覆盖以下一级模块：

1. 仪表盘
2. 业务管理：客户、借款申请、借款借据、还款计划、放款记录、还款订单、逾期、催收、优惠券
3. 财务与运营：费用调整、账务流水、Munsalik 对账
4. 业务配置：资金产品、支付机构、金额期数规则、App 资源位
5. 系统管理：管理员、角色、权限、审计日志

不在当前基线内：

- 独立的授信审核菜单
- 独立的账单金额明细菜单
- 独立的费用变更记录菜单
- 独立的还款分配菜单
- 独立的提前结清菜单
- 页面内直接销账、冲正、补账、退款或修改对账业务数据
- 对账文件手工上传及邮件隔离区处置

## 2. 功能状态说明

| 状态 | 含义 |
|---|---|
| 原型已覆盖 | 当前 `index.html` 已有页面、字段或可操作交互 |
| 待后端接入 | 前端已有模拟交互，仍需真实接口、持久化、权限和审计 |
| 待实现 | 原型没有对应入口或操作，不作为当前已完成功能 |

当前交付整体状态为“原型已覆盖、待后端接入”。

## 3. 角色与数据权限

| 角色编码 | 角色名称 | 主要职责 |
|---|---|---|
| `SUPER_ADMIN` | 超级管理员 | 全部菜单、配置及系统管理 |
| `RISK_REVIEWER` | 风控审核员 | 客户资料、授信审核、借款审核 |
| `CUSTOMER_SERVICE` | 客服 | 客户与订单查询、基础客户操作 |
| `COLLECTION` | 催收员 | 催收任务、跟进记录、承诺还款 |
| `FINANCE` | 财务 | 费用调整、账务流水、对账处理 |
| `OPERATIONS` | 运营 | 优惠券、产品规则、App 资源位 |

权限采用“菜单 + 操作”两层控制。当前原型已具备管理员、角色、权限树及分配权限的交互，但业务页面按钮尚未全部根据权限动态显隐；后端必须对所有敏感操作再次鉴权。

敏感字段包括手机号、证件号、联系人手机号和钱包账号。客户详情提供“显示敏感信息”交互；默认值必须脱敏，查看完整值需要独立权限并记录审计。

## 4. 信息架构

```text
仪表盘

业务管理
├── 客户
├── 借款申请
├── 借款借据
├── 还款计划
├── 放款记录
├── 还款订单
├── 逾期管理
├── 催收管理
└── 优惠券

财务与运营
├── 费用调整
├── 账务流水
└── Munsalik 对账

业务配置
├── 资金产品
├── 支付机构
├── 金额期数规则
└── App 资源位

系统管理
├── 管理员用户
├── 角色管理
├── 权限管理
└── 审计日志
```

## 5. 功能需求

### 5.1 仪表盘

**当前展示指标**

- 总客户数
- 在贷余额
- 逾期订单数
- 待审核授信数
- 在贷订单数
- 今日放款金额
- 今日还款金额
- 待审核费用调整数
- 按时还款率
- 逾期率
- 待处理对账数

**口径要求**

- 金额统一使用 PKR。
- 日期与自然日统计采用 PKT（UTC+5）。
- 当前为静态原型数据；接入后端时需为每个指标明确统计表、状态范围和刷新频率。

### 5.2 客户管理与授信审核

#### 5.2.1 客户列表

**查询条件**

- 客户 ID、姓名、手机号或证件号关键字
- 授信状态
- 黑名单状态

**列表字段**

- 客户 ID、姓名、手机号、证件号
- 授信状态、授信额度信息、钱包类型
- 黑名单状态、注册时间

**操作**

- 查看客户详情
- 加入或移出黑名单
- 对待审核客户执行授信通过、拒绝或转人工复核
- 显示或隐藏敏感字段

当前没有独立“授信审核”菜单，授信审核入口合并在客户列表及客户详情。

#### 5.2.2 客户详情

详情采用页签展示：

- 基础信息：客户 ID、姓名、手机号、证件号、城市、注册时间、授信汇总及黑名单状态
- 钱包账户：支付机构、脱敏账号、验证状态、验证时间
- 身份认证：证件材料、生日、性别、省市、地址等
- 紧急联系人：关系、姓名、手机号
- 授信申请历史：申请单号、提交时间、当前步骤、审核类型、审核结果、额度、有效期、拒绝原因和备注
- 授信变更记录：变更类型、金额、变更前后额度、关联单号、备注和时间

#### 5.2.3 授信审核规则

- 通过：更新授信审核结果及额度。
- 拒绝：拒绝原因必填。
- 转人工复核：记录人工复核状态。
- 所有操作必须写入后台审计日志；额度变化同步写入授信变更记录。
- 当前原型只模拟状态变更，真实持久化和并发控制待后端实现。

### 5.3 借款申请

**数据源：** `loan_apply_record`

**查询条件**

- 申请单号或客户 ID
- 申请状态
- 是否快速审核
- 风控状态

**主要字段**

- 申请单号、客户 ID、产品 ID
- 申请金额、提额券号及提额金额
- 分期期数、每期天数、总借款天数
- 日利率、日服务费率、日逾期费率
- 总利息、总服务费、理论应还、抹零金额、展示应还
- 前置验证状态、风控状态、申请状态
- 收款账户、放款状态、放款单号及放款时间
- 拒绝原因、审批时间、创建时间

**操作**

- 查看申请详情
- 审核通过
- 审核拒绝，拒绝原因必填

**与当前原型的边界**

- 当前页面没有独立“发起放款”按钮；放款执行由后端流程负责，页面通过放款状态和放款记录查看结果。
- 状态值与数据库数值映射由后端统一提供。

### 5.4 借款借据

**数据源：** `loan_voucher`

借据仅在放款成功后生成，不承担借款审批。

**查询条件**

- 借据号、申请单号或客户 ID
- 借据状态

**列表字段**

- 借据号、申请单号、放款单号、客户 ID
- 借款次数、借款金额、期数与周期
- 借据状态、已还金额、剩余金额、结清类型

**详情字段**

- 产品及支付账户关联信息
- 借款金额、期限、费率、理论应还及抹零
- 冷静期、费用封顶、累计费用
- 已还本金/利息/服务费/逾期费
- 剩余本金/利息/服务费/逾期费
- 结清状态、跑批时间、创建和更新时间

**操作**

- 查看详情
- 查看合同
- 下载合同
- 打印合同
- 对符合条件的未结清借据打开提前结清入口

提前结清不是独立菜单。当前操作只生成并展示提前结清号；结清金额计算、支付与状态落库应对接 `loan_early_settlement_record`。

### 5.5 还款计划

**数据源：** `loan_voucher_bill`

**查询条件**

- 账单号、借据号或客户 ID
- 账单状态
- 是否逾期

**列表字段**

- 账单号、借据号、客户 ID
- 期次、账期、账单状态
- 应还、已还、剩余金额
- 逾期天数、到期日、结清类型

**详情页签**

1. 账单金额：本金、利息、服务费、逾期费的应付、已付和剩余金额
2. 费用变更：费用类型、变更类型、来源、来源单号、变更前后金额、实付变化、减免金额、原因、操作人、备注和时间

账单金额明细与费用变更记录均合并在账单详情，不设置独立菜单。费用变更来源使用 `loan_bill_fee_changelog`。

### 5.6 放款记录

**数据源：** `loan_disbursement_record`

**查询条件**

- 放款单号、申请单号、客户 ID 或渠道流水号
- 放款状态
- 放款渠道

**列表字段**

- 放款单号、申请单号、客户 ID
- 申请金额、实际放款金额
- 渠道、支付账户 ID、渠道流水号
- 状态、成功时间、回调时间、失败原因、请求时间

当前页面为只读列表，没有详情弹窗和失败重试操作。失败重试如需建设，应新增操作权限、重试原因、幂等控制及完整审计，不计入当前功能。

### 5.7 还款订单与分配明细

**数据源：** `loan_repayment_order`、`loan_repayment_allocation`

**查询条件**

- 还款单号、借据号或客户 ID
- 还款状态
- 还款类型

**列表字段**

- 还款单号、借据号、客户 ID
- 还款类型、实际还款金额
- 支付机构、支付流水号
- 状态、支付时间

**详情页签**

1. 订单概要：剩余应还、券后应还、实际还款、优惠券、是否允许部分还款、状态和时间
2. 支付信息：付款机构、付款人、付款账号、支付流水、回调时间、成功时间、失败原因和失效时间
3. 费用分配：账单号、期次，以及本金、利息、服务费、逾期费和合计分配金额

还款分配仅在还款订单详情中展示，不设置独立菜单。

### 5.8 逾期管理

页面包含两个页签：

1. 逾期订单
2. 每日逾期记录

**逾期订单**

- 查询：借据号或客户 ID、逾期天数分层、催收员
- 字段：借据号、客户 ID、逾期本金、逾期天数、逾期费、剩余应还、到期日、渠道、催收员
- 操作：查看借据详情、发送提醒

**每日逾期记录**

- 数据源：`loan_overdue_record`
- 字段：逾期记录号、借据号、账单号、客户 ID、期次、逾期日期、逾期天数、逾期费率、逾期本金、费用金额、原始费用、入账时间

当前“发送提醒”为前端提示，短信、WhatsApp 或 Push 通道调用和发送结果记录待后端接入。

### 5.9 催收管理

**角色：** `COLLECTION`、`SUPER_ADMIN`

**概览指标**

- 催收任务数
- 今日跟进数
- 待履约承诺数
- 逾期 30 天以上任务数

**催收任务页签**

- 查询：借据号/客户 ID、逾期天数、催收员、最近跟进结果
- 字段：借据号、客户 ID、逾期天数、逾期金额、到期日、承诺还款日、最近结果、最近联系时间、催收员
- 操作：记录跟进、分配催收员

**跟进记录页签**

- 查询：借据号/客户 ID、联系结果、催收员
- 字段：跟进编号、借据号、客户 ID、联系时间、联系方式、结果、承诺日期、备注、催收员
- 操作：新增跟进；结果为“承诺还款”时填写承诺日期和金额并生成承诺记录

**承诺还款页签**

- 查询：借据号/客户 ID、履约状态、承诺日期
- 字段：承诺单号、借据号、客户 ID、承诺金额、承诺日期、状态、创建时间、催收员
- 操作：标记履约、标记违诺

当前数据为前端模拟；正式实现需要催收任务、分配、跟进和承诺还款的数据模型及接口。

### 5.10 优惠券

页面包含四个页签：

1. 提额券
2. 还款优惠券
3. 优惠券模板
4. 使用记录

**数据源：** `loan_coupon_template`、`loan_coupon_instance`、`loan_coupon_log`

**发放方式：** 提额券与还款优惠券均通过 `customer_id` 名单批量导入，不提供逐客户手工发放。导入后先整批预检客户存在性、重复、黑名单及券种资格；全部通过后才可确认发放，一名客户生成一张券。批次需保存导入文件、操作人、总数、成功/失败数及逐行失败原因，并使用批次号与幂等键防止重复发放。

**优惠券模板：** 支持新增和编辑，字段严格对齐 `loan_coupon_template`，不包含任何客户或用户字段，也不新增表内不存在的状态、备注字段。已发券模板不可修改券类型；批量发券只能选择当前券种下未删除且未过期的模板，并自动带出模板默认金额、有效期和规则。权限为 `coupon:template:view/create/edit`。

**提额券**

- 状态：`Unused`、`Locked`、`Used`、`Expired`、`Invalid`
- 操作：发券、对 `Unused` 券作废
- 提额金额仅参与一次借款额度校验，不修改永久授信额度，不冲减账单费用
- 券号使用 `LI` 前缀

**还款优惠券**

- 仅抵扣当前未还本金，不抵扣或重算利息、服务费、逾期费。
- 发放时绑定客户当前未结清借据；无可绑定借据或不能唯一确定目标借据时预检失败。
- 仅支持绑定借据全额还款，实际支付金额必须大于未还本金 50%。
- 支付前锁定，支付成功核销；取消、失败、超时或还款订单未成功时退回。

- 字段包括券金额、最低剩余本金、抵扣类型、适用还款类型、有效期和使用状态
- 操作：发券、作废

**优惠券模板**

- 当前原型提供模板列表展示。
- 模板新增、编辑或启停操作未在当前页面提供，属于待实现能力。

**使用记录**

- 展示锁定、使用、退回、过期和作废等过程记录。

### 5.11 费用调整

**数据源：** `loan_fee_adjust`

**查询条件**

- 调整单号或借据号
- 审核状态
- 费用类型

**字段**

- 调整单号、借据号、账单号、客户 ID
- 费用类型、调整类型、调整金额
- 调整前后金额、原因、申请人
- 审核状态、审核人、审核时间、创建时间

**操作**

- 创建费用调整
- 审核通过
- 审核拒绝

审批通过后应更新账单金额并生成 `loan_bill_fee_changelog`。当前原型只模拟列表状态变化，事务一致性待后端实现。

### 5.12 账务流水

**查询条件**

- 流水号、借据号或业务单号
- 业务类型
- 收支方向

**字段**

- 流水号、借据号、业务类型、业务单号
- 方向、金额、账户类型、账户 ID
- 状态、交易时间

当前页面为只读列表，没有详情和导出按钮。导出能力属于待实现，不计入当前功能。

### 5.13 Munsalik 放款与还款对账

一期目标是查看自动对账结果并进行轻量人工状态管理，不在页面内修改放款、还款或账务数据。

#### 5.13.1 主页面

包含三个页签：

1. 对账任务
2. 邮件隔离区
3. 对账审计

**对账任务**

- 查询：任务号、文件名、Message-ID、SHA-256、文件类型、来源、任务状态、收件日期
- 操作：重置筛选、导出当前结果、查看任务
- 字段：任务号、类型、覆盖日期、来源、发件人、收件时间、文件名、SHA-256、状态、总笔数、匹配数、差异数、待处理数

**任务详情页签**

- 任务概览
- 对账明细
- 差异明细
- 文件与邮件
- 操作记录

差异仅支持在 `PROCESSING` 与 `HANDLED` 之间切换。`HANDLED` 表示人工核实流程结束，不等同于数据已修正或双方已一致。

**邮件隔离区**

- 展示隔离邮件来源、主题、Message-ID、附件数量、哈希和隔离原因
- 当前严格只读，不提供解除隔离、作废、重试或下载操作

**对账审计**

- 查询操作人、动作和关联 ID
- 记录下载附件、导出、标记处理中、标记已处理和系统隔离等动作

**一期边界**

- 不按固定 T+1 展示或考核
- 不提供文件上传
- 不提供销账、冲正、补账、退款和调账审批
- 不提供规则或模板配置
- 原附件可在任务详情下载，下载需记录审计

### 5.14 资金产品配置

**数据源：** `loan_product`

**查询条件**

- 产品名称
- 状态
- 适用用户类型

**字段与配置**

- 产品名称、币种、用户类型、风险等级、排序、状态、备注
- 最低/最高金额、金额步长
- 最小/最大/默认期数、每期天数、最大总期限
- 日利率、日服务费率、日逾期费率、冷静期
- 提前还款、提前结清、部分还款
- 最大逾期天数、逾期费封顶、还款冲抵规则、取整规则

**操作**

- 新增
- 编辑
- 启用或禁用

保存时校验金额范围、期数范围和最大总期限；费率允许为 0。

### 5.15 支付机构配置

**数据源：** `loan_payment_institution`

**查询条件**

- 机构名称或 IBFT IMD
- 机构类型
- 目录状态

**字段**

- 原始机构名称、App 展示名称
- 机构类型、IBFT IMD
- 目录状态、客户是否可选、图标地址

**操作**

- 新增
- 编辑
- 启用或排除

`ibft_imd` 必须按字符串保存，最多 11 位数字，保留前导零。机构被排除时同时关闭客户可选状态。

### 5.16 借款金额期数规则

**数据源：** `loan_product_term_rule`

**字段**

- 产品 ID
- 最低/最高借款金额
- 可用期数
- 默认期数
- 启用状态
- 创建和更新时间

**操作**

- 新增
- 编辑
- 启用或禁用
- 删除

同一产品的启用金额区间不得重叠；每期天数从资金产品读取，不在规则中重复配置。

### 5.17 App 资源位

**角色：** `OPERATIONS`、`SUPER_ADMIN`

页面包含“资源位配置”和“变更记录”两个页签。

**资源位配置**

- 查询：位置 Key/名称、类型、状态
- 字段：名称、Key、类型、跳转类型、跳转链接、图片 URL、显示文案、排序、生效/失效时间、目标用户、状态、更新时间
- 操作：新增、编辑、启用或禁用
- Key 创建后不可修改

**变更记录**

- 查询：位置 Key/操作人、变更日期
- 字段：日志 ID、资源位、动作、时间、操作人、变更前后快照和备注

当前为前端模拟数据；正式实现需补资源位与变更日志表、接口和 App 拉取缓存策略。

### 5.18 系统管理

#### 管理员用户

- 查询：用户名/姓名、状态、角色
- 操作：创建、编辑、重置密码、启用/禁用、删除
- 新密码只允许展示一次

#### 角色管理

- 查询：角色关键字
- 操作：创建、编辑、分配权限、启用/禁用、删除
- 已分配用户的角色禁止删除

#### 权限管理

- 树形展示菜单与操作权限
- 操作：展开/收起、新增子权限、编辑、删除
- 权限编码创建后不可修改；被角色使用的权限禁止删除

#### 审计日志

- 查询：操作人/动作、模块、结果
- 字段：操作时间、操作人、模块、动作、目标、IP 和结果
- 后端应补充请求参数、前后快照及失败原因等完整审计信息

## 6. 数据表对照

| 业务模块 | 主要数据表 |
|---|---|
| 客户与授信 | `usr_account`、`usr_credit`、`usr_credit_basic_info`、`usr_credit_identity`、`usr_credit_contact`、`usr_credit_change_log` |
| 借款申请 | `loan_apply_record` |
| 借款借据 | `loan_voucher` |
| 还款计划与费用变更 | `loan_voucher_bill`、`loan_bill_fee_changelog` |
| 放款 | `loan_disbursement_record` |
| 还款 | `loan_repayment_order`、`loan_repayment_allocation` |
| 提前结清 | `loan_early_settlement_record` |
| 逾期 | `loan_overdue_record` |
| 费用调整 | `loan_fee_adjust` |
| 优惠券 | `loan_coupon_template`、`loan_coupon_instance`、`loan_coupon_log` |
| 产品与支付 | `loan_product`、`loan_product_term_rule`、`loan_payment_institution`、`loan_payment_account` |
| 系统权限 | `sys_admin_user`、`sys_role`、`sys_permission`、`sys_user_role`、`sys_role_permission`、`sys_operation_log` |

说明：催收、App 资源位、Munsalik 对账和账务流水的完整建表脚本不在当前 `zariya_loan.sql` 中，正式研发前需补充数据模型。

## 7. 安全与非功能要求

- 默认英文界面，支持中文切换。
- 业务时区为 PKT（UTC+5），币种为 PKR。
- 金额展示保留两位小数；前后端传输应避免浮点精度问题。
- 列表默认每页 20 条；当前原型分页为静态展示，真实分页待接口接入。
- 状态枚举由后端返回稳定编码，前端负责文案映射。
- 连续登录失败 5 次锁定 30 分钟；登录空闲超时 2 小时。
- 密码采用 bcrypt 或同等级算法存储。
- 敏感字段默认脱敏；查看完整值、导出、审批和状态变更必须记录审计。
- 删除采用软删除，关键金融记录不得物理删除。
- 所有新增、编辑、审核和状态切换接口必须支持幂等、并发校验和明确错误反馈。

## 8. 当前差距与研发待办

### P0：上线前必须完成

- 将全部前端模拟数据替换为真实 API，并完成分页、空态、加载态和错误态。
- 建立完整权限编码，覆盖所有侧边栏页面及关键按钮；前后端双重鉴权。
- 统一 PRD、前端路由、接口路径、数据库表和枚举命名。
- 完成客户敏感信息脱敏、解密查看授权与审计。
- 完成授信审核、借款审核、黑名单、费用调整等写操作的持久化、并发控制和审计。
- 对金额计算、账单更新、还款分配、优惠券锁定/退回使用数据库事务。

### P1：当前原型已有入口，需补后端链路

- 合同真实文件生成、存储、下载与打印。
- 提前结清金额计算、还款单创建及结清状态流转。
- 催收任务、跟进、分配和承诺还款的数据模型。
- App 资源位配置、变更日志及客户端拉取缓存。
- Munsalik 邮件接收、文件解析、匹配、差异状态和审计接口。
- 提醒通道接入与发送结果记录。

### P2：原型未覆盖，需单独评审

- 放款失败重试。
- 账务流水导出。
- 优惠券模板新增、编辑和启停。
- 对账邮件隔离处置。
- 独立提前结清工作台或独立还款分配查询页。

## 9. 验收原则

- 验收以本版信息架构和页面边界为准，不再按旧文档中的独立菜单验收。
- 页面展示字段必须与接口字段及数据库口径一一对应。
- 写操作必须验证角色权限、业务状态、必填信息、并发版本和幂等键。
- 任何金额变化都必须可追溯到业务单号、操作人、前后值和时间。
- 中英文切换后，菜单、筛选项、状态、按钮和弹窗文案保持完整一致。
- 原型存在但尚未接入后端的交互，不得仅以 Toast 或内存数据变化作为验收通过依据。

---

## 附录 A：本次对齐的主要调整

| 原文档表述 | 对齐后的口径 |
|---|---|
| 授信审核为独立功能模块 | 合并在客户列表与客户详情 |
| 账单金额明细为独立页面 | 合并在还款计划详情 |
| 费用变更记录为独立入口 | 合并在还款计划详情页签 |
| 还款分配为独立菜单 | 合并在还款订单详情页签 |
| 放款记录支持失败重试 | 当前仅只读查询，重试列入待实现 |
| 账务流水支持导出 | 当前无导出入口，列入待实现 |
| 提前结清未形成明确页面口径 | 入口位于借据详情，不设置独立菜单 |
| 邮件隔离区可执行处置 | 当前严格只读 |
| 对账差异包含复杂人工处理流程 | 当前仅支持“处理中/已处理”轻量状态 |
