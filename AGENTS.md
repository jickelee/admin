# AGENTS.md

## 项目定位

本项目是一套面向巴基斯坦现金贷业务的贷后管理后台 Demo，用于产品方案展示、业务流程评审和交互验证。

- 当前交付物是可浏览、可点击、可演示的产品设计页面，不是生产系统。
- 不要求真实后端、真实数据库、真实鉴权或真实支付能力；除非任务明确要求，否则使用前端状态与 Mock 数据完成演示闭环。
- 页面必须体现真实贷后业务逻辑，不能只做静态视觉稿。
- 业务口径优先参考根目录现有 PRD、SQL 和说明文档；发生冲突时，先指出冲突并采用最新、最明确的口径。
- 目标用户是催收、客服、财务、运营、风控主管和管理员等内部岗位。

## 默认工作原则

1. 先确认本次任务解决的业务问题、目标角色和演示路径，再开始设计页面。
2. 先保证信息架构、业务状态和操作闭环正确，再优化视觉表现。
3. 优先复用现有后台框架、组件和视觉语言，避免单个页面形成孤立风格。
4. 对缺失信息做合理假设并明确记录；会影响资金、状态或权限的关键假设不得悄悄带过。
5. Demo 中的按钮应尽量可操作：筛选、分页、切换、弹窗、抽屉、确认、成功/失败反馈等都要有对应状态。
6. 不为“看起来完整”虚构无法解释的业务能力。新增字段、指标或操作时，需要说明业务含义。
7. 除非明确要求，不建设超出 Demo 所需的后端、数据库、登录、消息队列或第三方接口。

## 产品岗位（Product）

产品岗位负责定义“做什么、为什么做、业务是否成立”。

### 核心职责

- 明确页面服务的业务角色、使用场景、核心任务和成功标准。
- 梳理贷后主链路：放款成功 → 生成借据/账单 → 到期提醒 → 正常还款或逾期 → 催收跟进 → 费用调整/减免 → 结清或核销。
- 定义页面信息架构、菜单归属、列表字段、筛选项、详情分区和操作入口。
- 定义业务对象之间的关系，包括客户、借款申请、借据、账单、还款订单、逾期记录、催收记录、费用调整和对账记录。
- 定义状态机、状态文案、可执行操作、前置条件和操作后的状态变化。
- 区分查看权限、操作权限、审批权限和敏感信息权限。
- 为设计岗位提供清晰的页面需求与验收标准。

### 产品输出要求

每个新页面或重要改版至少明确：

- 页面名称与业务目标
- 主要使用角色
- 入口与返回路径
- 核心演示场景
- 列表字段、筛选项、排序与默认值
- 详情信息分组
- 关键操作及前置条件
- 状态、枚举和颜色语义
- 空数据、加载、失败、无权限等异常状态
- 操作反馈及审计信息
- Mock 数据要求
- 可验证的验收标准

### 贷后业务检查

- 金额拆分是否清楚：本金、利息、服务费、逾期费、减免、已还、待还。
- 账单与借据、还款与分配、逾期与催收之间是否能追溯。
- 到期日、逾期天数、承诺还款日、结清时间等时间口径是否一致。
- 费用调整、减免、核销等高风险操作是否包含原因、凭证、审批和审计记录。
- 状态改变后，按钮是否仍然符合业务条件。
- 批量操作是否明确选中范围、影响数量和不可处理项。
- 敏感字段是否默认脱敏。
- 金额默认使用 PKR，业务时间默认使用 PKT（UTC+5）；若页面采用其他口径，必须明确标注。

## 设计岗位（Design）

设计岗位负责定义“如何让用户快速理解、准确操作，并适合演示”。

### 核心职责

- 在现有后台视觉体系内设计页面，不随意改变导航、间距、色彩、字体和组件语言。
- 围绕高频任务组织信息层级，优先展示状态、金额、时间、风险和下一步动作。
- 为复杂贷后信息选择合适形态：列表用于扫描，抽屉用于快速查看，详情页用于完整追溯，弹窗用于短流程操作。
- 设计完整交互状态，包括默认、悬停、选中、禁用、加载、成功、失败、空状态和无权限状态。
- 让关键操作可发现，但避免用过多高饱和颜色制造视觉噪音。
- 确保演示页面在常见桌面宽度下布局稳定，表格信息可读。

### 设计规范

- 后台以清晰、高密度、可扫描为主，不做营销页式的大标题和装饰性留白。
- 页面标题区包含必要的标题、说明、主操作和关键状态，不重复堆叠同一信息。
- 筛选区优先展示常用条件，低频条件放入“更多筛选”。
- 表格首屏优先呈现识别对象与判断状态所需的字段；低频字段放入详情。
- 长表格应考虑固定关键列、横向滚动、分页和批量选择反馈。
- 状态标签的颜色语义保持一致：
  - 绿色：成功、正常、已结清
  - 蓝色：进行中、待处理
  - 橙色：临期、需关注
  - 红色：逾期、失败、高风险
  - 灰色：失效、关闭、不可用
- 危险操作不得与普通操作同等强调，必须有二次确认及后果说明。
- 表单在字段附近展示校验提示；提交失败时保留用户已填内容。
- 数字右对齐，金额使用千分位，日期时间格式统一，缺失值统一使用 `--`。
- 文案应直接、专业、可执行，避免含糊词，如“处理一下”“异常数据”等。

### Demo 交互要求

- 使用稳定、具有关联性的 Mock 数据，避免同一客户或订单在不同页面出现矛盾。
- 关键按钮点击后必须产生可见结果，例如状态更新、Toast、弹窗关闭、记录新增或数据刷新。
- 至少覆盖一条正常链路和一条异常/逾期链路。
- 可以模拟接口延迟和结果，但不能让用户误以为已接入真实服务。
- 无真实能力的入口应标注“Demo”或提供合理的模拟反馈，不留下无响应按钮。
- 不用纯占位图替代关键信息；图表需要真实标签、单位、图例和可解释的数据。

## 产品与设计协作流程

### 1. 任务理解

产品先输出简短任务定义：目标角色、业务问题、范围、关键流程和验收标准。设计检查是否存在信息或交互缺口。

### 2. 结构设计

产品给出字段、状态、操作和页面关系；设计给出页面层级、组件选择和核心交互。涉及资金或状态变化时，双方共同确认操作前后结果。

### 3. 页面实现

实现过程中保持现有组件和样式一致。若视觉方案导致业务信息丢失，优先修正信息结构；若产品需求造成页面不可用，设计应提出更合适的交互方案。

### 4. 联合验收

产品按业务链路验收，设计按视觉与交互一致性验收。至少检查：

- 页面入口和返回路径是否成立
- 列表、详情和操作数据是否一致
- 筛选、分页、弹窗、抽屉等交互是否可用
- 正常、异常、空、加载、禁用和失败状态是否覆盖
- 金额、日期、时区、状态和敏感信息展示是否统一
- 主流程能否在演示中顺畅完成

## 决策优先级

出现冲突时按以下顺序决策：

1. 资金、合规、权限与审计正确性
2. 业务流程与状态逻辑正确性
3. 目标角色的操作效率与可理解性
4. 项目既有交互和视觉一致性
5. 演示效果与视觉精致度

## 完成定义（Definition of Done）

一项产品设计任务只有同时满足以下条件才算完成：

- 需求范围和关键假设已明确。
- 页面覆盖核心业务场景，状态与操作逻辑自洽。
- 关键交互可点击并有反馈，没有无响应的主要按钮。
- Mock 数据真实、关联一致且适合演示。
- 页面与现有后台风格一致，并完成常见桌面尺寸检查。
- 已检查空、错、加载、禁用、无权限等必要状态。
- 金额、时间、枚举、脱敏和审计信息符合统一口径。
- 没有为了 Demo 擅自引入不必要的生产级工程。

## 沟通约定

- 面向用户说明时，先给结论，再说明关键取舍和待确认项。
- 对不确定业务口径使用“假设”标注，不把推测写成既定规则。
- 提交页面时说明：改了什么、演示路径、覆盖状态、已知限制。
- 若发现现有 PRD、页面与数据结构互相冲突，先列出冲突和影响，再给出推荐口径。
