# Anole Loan 巴基斯坦借贷业务后台 PRD

**版本：** V1.6
**日期：** 2026-07-20
**产品：** Anole Loan 内部运营后台
**适用范围：** 管理员权限、客户授信、借款申请、借款借据、还款计划、放款还款、逾期、催收管理、优惠券、财务对账、业务配置、App资源位管理

---

## 1. 背景与目标

Anole Loan 面向巴基斯坦用户提供移动端现金借贷服务。随着业务量增长，内部需要一套可控、可审计、可按角色分权的运营后台，支撑风控审核、客服查询、催收处理、财务对账、运营配置和系统管理。

本后台的核心目标：

- 建立标准 RBAC 权限体系，控制不同岗位可见菜单和可执行操作。
- 围绕用户、授信、借款、账单、还款、逾期、优惠券、配置等业务表，提供查询、处理和审计能力。
- 为研发对接接口、后续迭代和权限拆分提供统一产品口径。

---

## 2. 用户角色

| 角色编码 | 角色名称 | 职责范围 |
|---|---|---|
| SUPER_ADMIN | 超级管理员 | 拥有所有菜单和操作权限 |
| RISK_REVIEWER | 风控审核员 | 查看客户资料、授信审核、借款审核 |
| CUSTOMER_SERVICE | 客服 | 客户信息查询、基础客户操作、订单查询 |
| COLLECTION | 催收员 | 逾期订单查看、提醒、催收跟进 |
| FINANCE | 财务 | 放款、还款、费用调整、账务流水、对账处理 |
| OPERATIONS | 运营 | 优惠券、借款期数配置、活动相关配置 |

---

## 3. 数据范围

后台页面基于《表结构》中的以下业务表设计：

| 模块 | 主要表 |
|---|---|
| 客户与授信 | `usr_account`、`usr_credit`、`usr_credit_basic_info`、`usr_credit_identity`、`usr_credit_contact`、`usr_credit_change_log` |
| 借款与账单 | `loan_apply_order`、`loan_disbursement_record`、`loan_voucher`、`loan_voucher_bill`、`loan_bill_fee_changelog` |
| 还款与逾期 | `loan_repayment_order`、`loan_repayment_allocation`、`loan_overdue_info` |
| 费用调整 | `loan_fee_adjust` |
| 优惠券 | `loan_coupon_template`、`loan_coupon_instance`、`loan_coupon_log` |
| 授信与支付 | `loan_credit_quota`、`loan_payment_institution`、`loan_payment_account` |
| 配置 | `loan_product`、`loan_product_term_rule` |
| 系统 | `sys_admin_user`、`sys_role`、`sys_permission`、`sys_user_role`、`sys_role_permission`、`sys_operation_log` |

---

## 4. 权限设计

权限采用三级结构：一级菜单组 -> 二级菜单 -> 按钮/动作权限。菜单权限控制页面可见性，按钮权限控制具体操作。

```text
dashboard                                仪表盘

customer                                 客户管理
  customer:list                          客户列表
    customer:list:view                   查看详情
    customer:list:view_sensitive         查看敏感信息
    customer:list:blacklist              修改黑名单
    customer:list:export                 导出
  customer:credit                        授信审核
    customer:credit:view                 查看授信资料
    customer:credit:review               审批/拒绝授信

loan                                     借款管理
  loan:application                       借款申请
    loan:application:view                查看申请详情
    loan:application:review              审批/拒绝借款申请
    loan:application:disburse            发起放款
  loan:voucher                           借款借据
    loan:voucher:view                    查看借据详情
    loan:voucher:contract                查看借款合同
  loan:bill                              还款计划
    loan:bill:view                       查看账单
    loan:bill:amount_detail              查看金额明细
    loan:bill:fee_change                 查看费用变更记录
  loan:disbursement                      放款记录
    loan:disbursement:view               查看放款记录
    loan:disbursement:retry              重试放款
  loan:repayment                         还款订单
    loan:repayment:view                  查看还款
    loan:repayment:detail                查看还款分配明细
  loan:adjustment                        费用调整
    loan:adjustment:create               创建调整
    loan:adjustment:approve              审批调整

coupon                                   优惠券管理
  coupon:limit_increase                  提额券
    coupon:limit_increase:view           查看
    coupon:limit_increase:issue          发券
    coupon:limit_increase:void           作废
  coupon:repay                           还款优惠券
    coupon:repay:view                    查看
    coupon:repay:issue                   发券
    coupon:repay:void                    作废
  coupon:usage                           优惠券使用记录
    coupon:usage:view                    查看

finance                                  财务与对账
  finance:transaction                    账务流水
    finance:transaction:view             查看
    finance:transaction:export           导出
  finance:reconciliation                 Munsalik 对账
  finance:reconciliation:view          查看任务、明细及差异
  finance:reconciliation:export        导出对账结果
  finance:reconciliation:download      下载原附件
  finance:reconciliation:mark_handled  标记差异为处理中/已处理
  finance:reconciliation:audit         查看对账审计日志

config                                   业务配置
  config:amount_term_rule                借款金额期数规则
    config:amount_term_rule:view         查看
    config:amount_term_rule:create       新增
    config:amount_term_rule:edit         编辑
    config:amount_term_rule:delete       删除

system                                   系统管理
  system:user                            管理员用户
  system:role                            角色管理
  system:permission                      权限管理
  system:audit_log                       后台审计日志
```

---

## 5. 功能需求

### 5.1 仪表盘

- 展示关键指标：总客户数、在贷余额、逾期订单数、待审核授信、今日放款、今日还款、待处理费用调整、待处理对账。
- 所有金额使用 PKR。
- 指标需支持按自然日统计，时区采用 PKT（UTC+5）。

### 5.2 客户管理

**客户列表**

- 页面默认不展示客户列表，只有执行查询后才展示匹配结果。
- 仅支持三种精确查询方式：完整手机号、完整 CNIC、客户 ID；不支持按姓名查询、模糊查询或无条件查看全部客户。
- 查询结果字段：客户姓名、手机号、证件号、授信状态、总额度、已用额度、钱包类型、黑名单状态、注册时间。
- 操作：查看详情、加入/移出黑名单、导出。
- 证件号、手机号默认脱敏；具备敏感信息权限时可查看完整值。

**客户详情**

- 基础信息：客户 ID、姓名、手机号、证件号、城市、注册时间、黑名单状态。
- 钱包信息：同一客户允许绑定多个钱包，以表格展示 `id`、`customer_id`、`payment_institution_id`、`account_identifier_masked`、`account_status`、`verification_time`、`created_at`、`updated_at`；账号仅展示数据库返回的脱敏值。账户状态包括 `VERIFIED`、`FAILED`、`UNKNOWN`。
- 授信信息：授信状态、审核状态、额度、已用额度、锁定额度、审核类型、审核时间、拒绝原因。
- 身份认证：证件正反面、生日、性别、省市、详细地址。
- 联系人：关系、姓名、手机号。
- 授信记录：同一客户可能存在多次授信申请，客户详情中需独立展示 `usr_credit` 申请历史，字段包括申请单号、提交时间、授信状态、当前步骤（基础信息/身份认证/紧急联系人/等待审核/已完成）、审核类型、审核结果、审核时间、授信额度、已用额度、过期时间、下次可申请时间、拒绝原因、备注；申请中的记录必须能展示客户当前停留在哪一步。
- 授信变更：展示 `usr_credit_change_log`，字段包括变更类型、变更金额、变更前后额度、关联订单、备注、时间。

### 5.3 授信审核

- 展示待审核、已通过、已拒绝、人工复核等状态。
- 审核员可查看客户基础信息、身份信息、联系人、历史借款和授信变更。
- 操作：通过、拒绝、转人工复核。
- 拒绝必须填写原因。
- 所有审核操作写入后台审计日志和授信变更记录。

### 5.4 借款申请

- 数据源：`loan_apply_order`。
- 字段：申请单号、客户 ID、产品 ID、申请金额、提额券号、提额金额、分期期数、每期天数、总借款天数、日利率、日服务费率、日逾期费率、总利息、总服务费、理论应还金额、抹零金额、展示应还金额、是否快速审核、申请状态、前置验证状态、风控状态、拒绝原因、审批时间、收款账户 ID、放款状态、放款时间、放款单号、创建时间。
- 筛选：申请单号/客户 ID、申请状态、风控状态、放款状态、申请时间。
- 操作：查看详情、审批、拒绝、发起放款、查看放款记录。
- 审批、拒绝和发起放款只能在借款申请中操作；申请被拒绝或放款失败时，已锁定的提额券必须退回。
- 申请状态与 `state`、`pre_state`、`risk_state`、`disbursement_state` 的具体数值映射由后端统一提供。

### 5.5 借款借据

- 数据源：`loan_voucher`；仅放款成功后生成借据。
- 字段：借据号、申请单号、放款单号、客户 ID、产品 ID、借款次数、借款金额、分期期数、每期天数、总借款天数、日利率、日服务费率、日逾期费率、总利息、总服务费、理论应还金额、抹零金额、展示应还金额、起止日期、冷静期起止时间、费用上限、借据状态、累计费用、总逾期费、还款优惠券金额、已还本金/利息/服务费/逾期费、已还总额、剩余本金/利息/服务费/逾期费、剩余总额、是否费用封顶、结清类型、结清时间、最后跑批时间、创建时间。
- 筛选：借据号/申请单号/客户 ID、借据状态、结清类型、创建时间。
- 操作：查看详情、查看合同、查看还款计划。
- 借据不承担借款审批和发起放款操作。
- 借据状态建议展示为：`CoolingOff`、`Active`、`Overdue`、`Paid`、`WrittenOff`；具体数值映射由后端提供。

### 5.6 放款记录

- 数据源：`loan_disbursement_record`，直接关联申请单号 `apply_no`。
- 字段：放款单号、申请单号、客户 ID、产品 ID、申请金额、实际放款金额、放款渠道、收款账户 ID、渠道流水号、放款状态、失败原因、请求时间、成功时间、回调时间、原始回调报文。
- 筛选：放款单号/申请单号/客户 ID、状态、渠道、时间范围。
- 操作：查看详情、失败重试。
- 放款重试必须记录操作人、原因和结果。

### 5.7 还款计划

- 数据源：`loan_voucher_bill`。
- 字段：账单号、借据号、客户 ID、产品 ID、期次、周期、账单状态、应还本金/利息费用/服务费/逾期费、应还总额、已还本金/利息/服务费/逾期费、已还总额、剩余本金/利息费用/服务费/逾期费、剩余总额、逾期天数、结清类型、结清时间、跑批时间。
- 筛选：账单号/借据号/客户 ID、状态、逾期天数、到期日。
- 账单状态建议统一为：`CoolingOff`、`Active`、`PartiallyPaid`、`Overdue`、`OverduePartiallyPaid`、`Paid`、`EarlySettled`、`WrittenOff`。

### 5.8 账单金额明细

- 不使用 `loan_bill_fee`，金额明细直接来自 `loan_voucher_bill`。
- 每条账单按本金、利息、服务费、逾期费展示应付、已付、剩余金额。
- 字段：账单号、借据号、客户 ID、期次，以及四类金额的应付/已付/剩余值。
- 筛选：账单号/借据号/客户 ID。
- 每条结果提供“查看变更记录”按钮。

### 5.9 费用变更记录

- 数据源：`loan_bill_fee_changelog`，不设置独立菜单，从账单金额明细行内查看。
- 字段：记录 ID、借据号、账单号、客户 ID、产品 ID、期次、费用类型、变更类型、来源类型、来源单号、变更前后应付、变更前后已付、实付变更、变更前后剩余、减免金额、变更原因、操作人、备注、创建时间。
- 来源类型包括：逾期生成、人工调整、优惠券、还款分配。

### 5.10 还款订单

- 数据源：`loan_repayment_order`。
- 字段：还款单号、借据号、客户 ID、剩余应还金额、券后应还金额、优惠券号、优惠券金额、还款成功时间、还款类型、是否允许部分还款、状态、付款钱包机构编码、付款人姓名、付款账户号、实际还款金额、支付回调时间、还款时间、支付流水号、失败原因、失效时间、创建时间、更新时间。
- 筛选：还款单号/借据号/客户 ID/支付流水号、状态、付款钱包机构、时间范围。
- 操作：查看详情；详情内展示 `loan_repayment_allocation` 分配记录。
- 还款状态建议统一为：`Pending`、`Success`、`Failed`、`Cancelled`。

### 5.11 还款分配

- 独立菜单展示还款分配明细表。
- 字段：记录 ID、还款单号、借据号、账单号、客户 ID、期次、分配总额、已分配本金、已分配利息、已分配服务费、已分配逾期费、创建时间。
- 筛选：还款单号/借据号/账单号。
- 每条记录聚合展示四类费用的分配金额，不再使用产品 ID、费用类型和分配顺序。

### 5.13 费用调整

- 数据源：`loan_fee_adjust`。
- 字段：调整单号、借据号、账单号、客户 ID、产品 ID、费用类型、调整类型、调整金额、调整前后金额、原因、申请人、审批人、状态、创建时间。
- 支持创建费用调整，费用类型包括利息、服务费、逾期费。
- 支持审批通过/拒绝。
- 审批通过后生成费用变更记录，并同步账单剩余金额。

### 5.14 账务流水

- 字段：流水号、客户 ID、借据号、账单号、业务类型、业务单号、方向、金额、币种、账户类型、账户 ID、状态、交易时间、入账时间。
- 筛选：流水号/借据号/客户 ID、业务类型、方向、状态、时间范围。
- 支持导出。

### 5.15 Munsalik 放款与还款对账

本模块用于查看 Munsalik 放款（Disbursement）和还款（Collections）文件的自动对账结果。第一期只做后台页面，不在页面内执行销账、冲正、补账、退款、调账审批或直接修改业务数据。

**一期产品原则**

- Munsalik 文件发送频率不固定，可能每周一次或每周两次，不按照固定 T+1 展示或考核。
- 不建设对账工作台或数据概览；进入模块后默认展示对账任务列表。
- 不提供文件补录上传入口；补充文件或修正版统一通过白名单邮箱重新发送。
- 系统自动完成文件解析和交易匹配，页面不提供逐笔人工配对，避免数据量增加后人工匹配出错。
- 新文件到达并重新计算后，页面直接展示最新对账结果；不提供“关联补充任务”操作。
- 差异只保留“处理中、已处理”两个处理状态，不登记对方结论、问题归属、等待状态、修正工单或关闭结论。
- 邮件隔离区仅用于查看，不提供解除隔离、确认无效、重试、下载或其他处理功能。
- 我方数据确需修正时，在对账模块之外通过现有受控流程完成；本页面只展示结果和标记处理状态。

#### 5.15.1 页面结构

```text
Munsalik 对账
├── 对账任务
│   └── 任务详情
│       ├── 任务概览
│       ├── 对账明细
│       ├── 差异明细
│       ├── 文件与邮件
│       └── 操作记录
├── 邮件隔离区（只读）
└── 对账审计日志
```

#### 5.15.2 对账任务列表

**查询条件**

- 任务编号或关键字
- 业务类型：放款、还款
- 来源：Munsalik 官方邮件、我方白名单邮箱补发
- 收件时间范围
- 任务状态
- 关键字支持文件名、Message-ID、SHA-256 和发件人

**列表字段**

- 任务编号
- 业务类型
- 交易覆盖开始日期、结束日期
- 来源及发件人
- 收件时间
- 文件名及 SHA-256 摘要
- 任务状态
- 总笔数
- 匹配一致笔数
- 差异笔数
- 处理中笔数
- 已处理笔数
- 操作：查看详情

**页面操作**

- 查询、重置
- 导出当前查询结果（按权限显示）
- 查看任务详情

**任务状态**

| 状态 | 页面展示 |
|---|---|
| `PROCESSING` | 处理中 |
| `COMPLETED` | 已完成 |
| `VALIDATION_FAILED` | 校验失败 |
| `PROCESS_FAILED` | 处理失败 |
| `QUARANTINED` | 已隔离 |
| `DUPLICATE_IGNORED` | 重复文件已忽略 |
任务列表不展示“T+1 超期”或“日报漏发”状态。

#### 5.15.3 任务详情：任务概览

**基础信息**

- 任务编号、业务类型、交易覆盖日期
- 来源、任务状态
- 收件时间、开始处理时间、完成时间
- 总行数、有效行数和异常行数

**结果汇总**

- 总笔数
- 匹配一致笔数
- 差异笔数
- 处理中笔数
- 已处理笔数

校验或处理失败时仅展示任务表 `failure_reason`，第一期不增加结构化校验错误列表及错误清单导出。

#### 5.15.4 任务详情：对账明细

**查询条件**

- 我方订单号
- STAN
- RRN
- Loan ID/customer_id
- 对账状态

**列表字段**

- 明细编号
- 我方订单号
- STAN、RRN
- Loan ID/customer_id
- 我方金额、Munsalik 金额
- 我方渠道费用、Munsalik 渠道费用
- 我方状态、Munsalik 状态
- 交易日期
- 对账状态
- 操作：查看

**对账状态**

| 状态 | 页面文案 |
|---|---|
| `MATCHED` | 匹配一致 |
| `MISSING_PARTNER` | 我方有、对方无 |
| `MISSING_INTERNAL` | 我方无、对方有 |
| `AMOUNT_MISMATCH` | 金额/手续费不一致 |
| `STATUS_MISMATCH` | 状态不一致 |
| `DUPLICATE` | 重复记录 |

明细详情左右对比双方数据，不一致字段突出显示。原始对账数据只读，不允许在页面中编辑。

#### 5.15.5 对账匹配字段展示口径

还款对账：

| Munsalik 字段 | 我方字段 |
|---|---|
| Transaction Date | `payment_success_time` |
| Loan ID | `customer_id` |
| Amount | `repayment_amount` |
| STAN | `payment_txn_no` |
| RRN | `rrn` |
| Status | `payment_status` |

放款对账：

| Munsalik 字段 | 我方字段 |
|---|---|
| Date | `success_time` |
| Amount | `disbursement_amount` |
| Fee | `channel_fee` |
| STAN | `channel_txn_no` |
| RRN | `rrn` |
| Status | `disbursement_status` |

#### 5.15.6 任务详情：差异明细

**快捷筛选**

- 处理中：默认打开
- 已处理
- 全部

**查询条件**

- 明细编号或关键字
- 对账状态
- 关键字支持订单号、STAN 和 RRN

**列表字段**

- 明细编号
- 业务类型
- 对账状态
- 我方订单号
- STAN、RRN
- 我方金额、Munsalik 金额
- 我方渠道费用、Munsalik 渠道费用
- 处理状态
- 处理人
- 处理时间
- 更新时间
- 操作：查看、标记已处理/标记处理中

**处理状态**

| 状态 | 含义 |
|---|---|
| `PROCESSING` | 该差异尚未处理完成 |
| `HANDLED` | 运营已确认该差异已处理 |

异常生成后默认状态为 `PROCESSING`。运营点击“标记已处理”后直接变为 `HANDLED`，记录操作人、处理时间和状态变更日志；如误操作，可点击“标记处理中”恢复。状态切换不要求填写原因、备注、凭证或结论。

差异详情只展示双方数据对比、当前处理状态、处理人、处理时间、更新时间、Sheet/行号和状态时间线。页面不提供等待对方、等待补充文件、转数据修正、销账、冲正、调账、审批或关闭差异按钮。

#### 5.15.7 文件与邮件页签

**邮件信息**

- 合作方编码、来源类型
- Sender、Recipient、Subject
- Message-ID、收件时间、邮件状态、邮件 SHA-256

**文件信息**

- 文件名、文件大小、SHA-256
- 交易覆盖开始日期、结束日期
- 总行数、有效行数、异常行数

**操作**

- 下载原附件
- 复制 Message-ID
- 复制 SHA-256

页面仅保留原附件下载，不提供邮件原文展示、上传、替换、编辑或删除入口。原附件下载根据权限显示并写入审计日志。

#### 5.15.8 邮件隔离区（只读）

**查询条件**

- 发件人、Message-ID、邮件主题
- 隔离原因

**列表字段**

- 邮件编号
- 发件人、收件人、Subject、Message-ID
- 附件数量
- 隔离原因、收件时间、邮件状态
- 操作：查看详情

隔离详情展示邮件表已有字段和隔离原因。该页面只做展示，不提供查看或下载原附件、解除隔离、确认无效、重新处理、编辑或删除等操作。异常邮件后续如何处置由系统或页面外流程完成。

#### 5.15.9 操作记录与对账审计日志

任务内“操作记录”展示当前任务相关日志；“对账审计日志”展示全部对账操作。

**查询条件**

- 操作人或关键字
- 动作

**主要动作**

- 下载原附件
- 标记处理中
- 标记已处理
- 导出对账结果
- 系统隔离邮件

**列表字段**

- 日志编号
- 操作时间、操作人 ID、操作人名称
- 动作、关联邮件 ID、任务 ID、明细 ID
- 操作前状态、操作后状态、IP、操作补充信息

#### 5.15.10 页面权限约束

| 页面/操作 | 对账查看员 | 对账经办人 | 对账管理员 | 审计查看员 |
|---|---:|---:|---:|---:|
| 查看任务、明细和差异 | 是 | 是 | 是 | 是 |
| 导出对账结果 | 按授权 | 按授权 | 是 | 按授权 |
| 标记处理中/已处理 | 否 | 是 | 是 | 否 |
| 下载原附件 | 按授权 | 按授权 | 是 | 按授权 |
| 查看邮件隔离区 | 是 | 是 | 是 | 是 |
| 在邮件隔离区执行处理 | 否 | 否 | 否 | 否 |
| 查看对账审计日志 | 否 | 否 | 是 | 是 |

### 5.16 优惠券管理

**提额券**

- 业务定义：提额券在有效期内为客户提供一次性可借额度增量，不抵扣借款本金、利息或费用；借款申请占用时进入 `Locked`，放款成功后进入 `Used`，借款审核拒绝或放款失败时退回为 `Unused`。
- 数据模型：`loan_coupon_template` 保存模板，`loan_coupon_instance` 保存客户持券，`loan_coupon_log` 保存锁定、使用、退回和失效记录。
- 字段：模板、券号、客户 ID、提额金额、状态、生效时间、失效时间、锁定申请单号、使用时间。客户基础授信额度由授信系统实时读取，不属于券配置。
- 状态：`Unused`、`Locked`、`Used`、`Expired`、`Invalid`。
- 发券方式：不支持后台逐个录入客户发券。运营下载名单模板后一次导入一批客户，模板必填列为 `customer_id`；系统先预检整批名单，全部通过后才允许确认发放，一名有效客户生成一张独立券。
- 批量预检：校验客户存在、客户 ID 格式、名单内重复、黑名单、授信状态；任一行失败时整批不可发放，页面逐行展示失败原因，避免部分成功导致运营无法核对。
- 发券校验：名单、提额金额、生效时间、失效时间必填；提额金额必须大于 0 且为 100 的整数倍（如 200、300、1000），失效时间必须晚于生效时间。
- 操作：批量发券、作废；仅 `Unused` 状态允许作废。批量任务和每张实例都需要记录操作人、导入文件名、总数、成功数、失败数和失败明细。
- 数据口径：可借额度按使用时实时计算为 `current_credit_limit + increase_amount`；券号前缀为 `LI`，提额券在借款申请阶段锁定 `apply_no`，放款成功后使用，申请拒绝或放款失败时退回，不直接修改客户永久授信额度。
- 存储口径：模板规则写入 `loan_coupon_template.rule_config`，客户持券写入 `loan_coupon_instance`，使用过程写入 `loan_coupon_log`；不再使用旧 `loan_coupon`/`repayment_coupon` 页面模型。

**还款优惠券**

- 业务定义：仅抵扣客户当前未还本金，不调整已产生或已入账的利息、服务费和逾期费。优惠后应还金额 = 当前未还本金 - 券金额 + 利息 + 服务费 + 逾期费。
- 字段：模板、券号、客户 ID、券金额、绑定借据号、绑定时未还本金快照、状态、生效时间、失效时间、锁定还款单号、使用/退回时间和退回原因。
- 发券方式：与提额券一致，通过名单模板批量导入；预检时除客户基础校验外，还必须校验客户存在当前未结清借据，并将券绑定该借据。无未结清借据或无法唯一确定目标借据的行不得发放。
- 使用约束：仅绑定借据可用；必须全额还款，不支持部分还款；支付金额必须大于账单未还本金的 50%；支付发起后进入 `Locked`，支付成功后进入 `Used`。
- 退回规则：取消支付、支付失败、支付超时或还款订单未成功时，优惠券退回客户账户，并记录原还款单号及退回原因。
- 操作：批量发券、作废；仅 `Unused` 状态允许作废。

**优惠券模板**

- 模板支持新增和编辑，不再是只读列表；页面与 `loan_coupon_template` 严格对齐，仅包含模板 ID、模板名称、券类型、券金额、产品 ID、生效/失效时间、已发放数量、规则、删除标识和创建/更新时间，不包含客户或用户字段。
- 提额券模板规则包括金额步长和最高借款额度 50,000 PKR；默认金额和金额步长必须为 100 的整数倍。
- 还款券模板规则固定包含仅抵扣本金、仅全额还款、实际支付金额大于本金 50%，并可配置最低剩余本金。
- 已产生券实例的模板不得修改券类型。
- 批量发券页仅展示当前券种下未删除、且尚未过期的模板；选择模板后自动带出默认金额、规则和有效期，运营可编辑的字段仍需按模板规则校验。
- 权限拆分为 `coupon:template:view/create/edit`，模板新增和编辑均写入审计日志。

**使用记录**

- 展示提额券及还款优惠券的锁定、使用、退回、过期全过程。
- 状态流转统一为：`Unused → Locked → Used`、`Unused → Locked → Returned`、`Unused → Expired`、`Unused → Invalid`。`Returned` 记录事件后，若业务允许再次使用，应将实例可用状态恢复为 `Unused`，日志仍保留 `Returned` 事件。
- 提额券的锁定金额、使用金额表示额度增量，权益类型为 `CreditLimit`；还款优惠券的抵扣类型固定为 `Principal`。

**本次变更评估与验收口径**

- 前端影响：券列表、批量发券与名单预检弹窗、批次结果、筛选、使用记录、中英文文案、权限树和运营角色权限需要同步调整。
- 接口影响：新增“上传/解析名单、批次预检、确认发放、批次结果/失败明细”能力。确认接口必须带预检批次号和幂等键，服务端在事务中防止重复发券；大批量名单应异步执行并提供批次状态查询。
- 接口影响：原借款券类型值 `loan` 调整为 `limit`，券号前缀由 `LC` 调整为 `LI`；服务端应在灰度期兼容旧值读取，禁止新发 `LC` 券。
- 权限影响：`coupon:loan:*` 迁移为 `coupon:limit_increase:*`，发布前须为已有运营角色补齐新权限，避免菜单不可见。
- 财务影响：提额金额不是费用减免，不得写入优惠券抵扣费用项或冲减账单；仅在借款额度校验中参与一次。
- 验收通过条件：批量名单可下载模板、导入、逐行预检并整批确认；重复提交不会重复发券；提额券列表正确显示提额金额和关联申请单；50,000 PKR 上限、100 倍数与有效期校验生效；不展示或存储客户基础额度；锁定/使用/退回记录口径正确；还款券仅抵扣本金、绑定未结清借据、只允许全额还款且满足实付大于本金 50%。

### 5.17 业务配置

**资金产品配置**

- 数据源：`loan_product`；产品 ID 为系统自增主键，在后台列表展示但不可编辑，创建时间与更新时间由系统维护。
- 基础字段：产品名称、币种、适用用户类型、风险等级、排序号、状态、备注。
- 金额与期限：最低/最高借款金额、金额步长、最小/最大/默认分期期数、每期天数、最大总期限。
- 费率与冷静期：日利率、日服务费率、日逾期费率、冷静期小时数、冷静期内是否免利息和服务费。费率按数据库小数值填写，例如 `0.006750 = 0.675%`。
- 还款策略：是否支持当期提前还款、提前还款开放天数、是否支持提前结清、是否支持部分还款、最低部分还款金额、最大逾期天数、逾期费封顶比例、还款冲抵规则、取整规则。
- 默认值：币种 `PKR`，金额步长 `1000`，冷静期 `24` 小时，还款冲抵规则 `OVERDUE_FEE_FIRST`，取整类型 `DOWN`，用户类型 `ALL`，状态为启用。
- 列表支持按产品名称、状态、适用用户类型筛选；操作包括新增、编辑、启用和禁用。
- 保存校验：最低金额必须小于最高金额；默认期数必须位于最小/最大期数之间；最大总期限不得小于 `最大期数 × 每期天数`；必填费率允许为 `0`。

**支付机构配置**

- 数据源：`payment_institution`；机构 ID 为系统自增主键，创建时间与更新时间由系统维护。
- 字段：合作方原始机构名称、App 展示名称、机构类型、IBFT IMD、目录状态、客户是否可选、机构图标地址。
- 机构类型：`BANK`、`MICROFINANCE_BANK`、`EMI_WALLET`、`OTHER`。
- 目录状态：`ENABLED`、`EXCLUDED`。`EXCLUDED` 机构保留在目录中用于审计，但不可供客户选择；将机构排除时同步关闭客户可选状态。
- `ibft_imd` 必须作为字符串存储和传输，最多 11 位数字，不得转为数值，避免丢失前导零。
- 列表支持按机构名称/IMD、机构类型、目录状态筛选；操作包括新增、编辑、启用和排除。
- 权限：`config:payment_institution:view/create/edit/toggle_status`。

**借款金额期数规则**

- 数据源：`loan_product_term_rule`。
- 字段：产品 ID、最低借款金额、最高借款金额、可用期数、默认期数、是否启用、创建时间、更新时间。
- 每期天数来自 `loan_product.period_days`，不在期数规则中重复配置。
- 规则需校验金额区间不得重叠。
- 仅启用状态的规则参与用户借款匹配。

### 5.18 催收管理

#### 5.18.1 产品定位与边界

催收模块服务内部自营催收团队，围绕触达策略、人工分案、坐席催收、PTP、人工登账和减免申请形成轻量闭环。

V1 不包含资产包导入导出、委外机构、法催、诉讼、外访、催收员之间流转、复杂自动分案、预测式外呼、完整呼叫中心、完整财务账务和复杂绩效薪酬。案件需要更换催收员时，只允许主管或管理员再分配。

#### 5.18.2 角色

| 角色 | 职责 |
|---|---|
| 催收主管/管理员 | 查看全局数据、配置策略、分案、再分配、审批减免 |
| 催收员 | 处理本人案件、拨号、填写催记、创建和跟进 PTP、提交人工登账和减免申请 |
| 财务确认人 | 确认或驳回人工登账 |
| 系统管理员 | 维护用户、角色、渠道、模板和权限阈值 |

#### 5.18.3 页面结构

```text
催收管理
├── 催收看板
├── 催收工作台
├── 分案管理
├── 策略配置
└── 业务处理
    ├── PTP 记录
    ├── 登账审核
    └── 减免审批
```

催收员默认进入催收工作台，主管默认进入催收看板，财务确认人默认进入登账审核。

#### 5.18.4 催收工作台

工作台使用“左侧任务列表 + 右侧案件工作区”双栏结构。

**任务分类**

- 今日待跟进
- 新分配
- PTP 今日到期
- PTP 已违约
- 多次未联系
- 全部在手案件

默认只展示当前催收员负责且尚未结清的案件，支持按客户/借据、逾期天数、金额、省、最近联系结果、PTP 状态和下次跟进时间筛选。

**案件摘要**

- 半脱敏客户姓名、客户编号、借据号
- 省、客户类型（新客/老客）
- 当前负责人
- 借款时间、总期数
- 当前逾期期次，例如“第 2 / 3 期”
- 存在连续多期逾期时，展示全部逾期期次及连续逾期数量，例如“连续逾期 2 期：第 2、3 期”；当前逾期期次取最早未结清逾期期次。
- 未结清期数；包含当前逾期期次和尚未到期但未结清的后续期次
- 到期日、逾期天数和逾期阶段
- 本金、利息、服务费、逾期费、剩余应还总额
- 已还本金、已还利息、已还服务费、已还逾期费、已还总额
- 当前 PTP 状态及履约进度

工作台顶部直接展示“拨打客户”“填写催记”“人工登账”“减免申请”四个操作按钮。人工登账和减免申请不收纳在“更多操作”菜单中；PTP 的创建和更新统一从独立 PTP 信息区进入。

四个操作按钮使用紧凑尺寸并保持单行排列。金额汇总下方固定展示独立 PTP 信息区：PTP 状态、承诺日期、承诺金额、已履约金额、剩余承诺金额和履约进度；没有 PTP 时展示明确空状态和创建入口。

工作台信息块按业务语义区分，但保持低饱和后台风格：金额概览使用靛蓝标识，PTP 使用橙色标识，借款与还款进度使用蓝色边框，催收时间线使用紫色顶部标识，客户与联系人使用绿色顶部标识。不同区域通过卡片边界、背景、间距和标题色形成层级，状态颜色仍遵循全局状态语义。

**还款计划与还款记录**

工作台将还款计划和实际还款记录合并展示。当前借据的完整还款计划每一期独立一行，并在对应期次内直接展示该期的全部实际还款流水，便于同时核对应还安排和到账情况。计划字段包括：

该区域默认收起，仅展示总期数、还款流水数量和“展开查看”按钮；点击后展示完整表格，用户可再次点击“收起”隐藏，避免工作台首屏信息过长。

- 期次
- 到期日
- 该期本金
- 该期利息
- 该期服务费
- 该期逾期费
- 该期应还金额
- 该期已还金额
- 该期剩余金额
- 期次状态：已结清、逾期、未到期

每期关联的还款流水展示还款单号、还款金额、实际还款/到账时间、还款渠道和还款状态；同一期存在多笔部分还款时逐笔保留，并按实际还款时间倒序排列。没有还款流水的期次展示“暂无还款”。

连续多期逾期时，所有逾期期次均使用逾期状态突出展示；部分还款只更新对应期次的已还和剩余金额，不得将该期错误标记为已结清。每笔还款还需同步反映到页面金额汇总和 PTP 累计履约金额。

**客户与联系人**

- 客户和紧急联系人姓名、关系、半脱敏手机号
- 具备敏感信息权限的用户可查看完整号码，查看行为写入审计日志
- 每个号码旁提供拨号按钮，并明确标识本人或联系人

**软电话**

- 点击拨号按钮连接客服软电话
- 默认返回接通状态和通话时长
- 展示呼叫中、已接通、未接、拒接、空号和失败等状态
- 通话结束后自动打开催记表单，并自动带入联系对象、接通状态和时长

**全渠道时间线**

按时间倒序统一展示短信、Push、AI 电话、人工电话、催记、PTP、人工登账、减免、分案和管理员再分配记录。AI 电话默认返回接通状态、通话时长、结果标签和 AI 通话小结。

**催记**

字段：

- 联系对象：本人/联系人
- 联系渠道
- 联系结果：已接通、未接、拒接、关机、空号、其他
- 沟通结果：承诺还款、暂时无力、拒绝还款、有争议、已还款、需再次跟进、其他
- 催收备注
- 下次跟进时间

规则：

- 沟通结果为“承诺还款”时继续创建 PTP。
- 沟通结果为“已还款”时可继续提交人工登账。
- 沟通结果为“需再次跟进”时，下次跟进时间必填。
- 已保存催记不允许催收员直接删除；需要修正时追加更正记录。

#### 5.18.5 分案与管理员再分配

分案管理同时承担催收主管查看团队案件执行情况的入口，不新增一级菜单。页面包含“案件明细”和“催收员视图”两个页签。

**筛选条件**

- 省
- 剩余应还金额区间
- 客户类型：新客/老客
- 逾期天数或逾期阶段
- 产品
- 是否历史逾期
- 是否已有 PTP
- 当前催收员/是否未分配
- 今日是否已跟进

**案件字段**

- 客户编号、半脱敏姓名和手机号
- 借据号
- 省、客户类型
- 逾期天数、逾期阶段
- 剩余本金、逾期费、剩余应还总额
- 最近触达结果
- 最近触达时间及渠道
- 最近电话接通状态和通话时长
- 最近人工催记
- PTP 状态
- 今日跟进次数
- 下次跟进时间
- 当前催收员

**主管案件明细**

- 默认展示主管权限范围内全部已分配和未分配案件，一行一个案件。
- 点击“详情”打开案件催收详情，按时间倒序展示短信、Push、AI 电话、人工电话、AI 通话小结、人工催记、PTP、登账、还款、减免和分案/再分配记录。
- 详情同时展示当前负责人、逾期天数、省、剩余应还、最近电话、PTP 和下次跟进时间。
- 对已进入催收工作台数据集的案件，主管可从详情直接进入催收工作台。
- 主管查看行为及再分配操作需要进入审计日志。

**催收员视图**

- 按催收员汇总当前案件数、今日已跟进/待跟进案件数、今日接通数、有效 PTP 数、违约 PTP 数和已确认回款金额。
- 点击某一催收员的“查看案件”，切换到案件明细并自动按该催收员筛选。
- 该视图仅用于主管了解执行情况，不设置案件负载上限，不阻止继续分案。

**分案方式**

- 单笔分配
- 勾选多笔批量分配
- 按筛选结果批量分配
- 退回待分配池

分配弹窗只展示启用状态的坐席，并提供以下辅助判断信息：

- 坐席姓名和所属组
- 当前负责案件总数
- 待跟进、跟进中、有效 PTP、PTP 违约案件数
- 使用分段柱状条展示上述案件状态分布，并同步展示各状态的准确数量

案件数量和状态分布只用于主管分案参考，不代表负载额度。坐席不设置案件负载或最大案件数限制，主管可直接选择任一启用坐席完成分配。

**管理员再分配**

- 主管或管理员可单笔或批量将已分配案件调整给新坐席。
- 必须填写再分配原因，提交后立即生效，无需原坐席或目标坐席确认。
- 目标坐席只校验账号处于启用状态，不校验在手案件数或负载上限。
- 原坐席尚未完成的普通跟进任务转给新坐席。
- 历史催记、触达、PTP、登账、减免记录全部保留。
- 生效中的 PTP、待审核登账和待审批减免不因负责人变化而失效。
- 再分配通知原坐席和新坐席，并写入案件时间线和审计日志。

催收员之间不允许直接流转案件。

#### 5.18.6 策略配置

策略表达“逾期 X 天，通过 X 渠道，向 X 对象发送 X 内容”。

**条件**

- 逾期天数：单日或区间
- 客户类型：新客/老客
- 剩余应还金额区间
- 省
- 是否存在有效 PTP
- 是否已结清

**动作**

- 渠道：短信、App Push、AI 电话、人工电话任务
- 触达对象：本人、紧急联系人
- 执行时间：PKT 业务时段
- 消息或话术模板
- 同一渠道每日最大触达次数

**防打扰规则**

- 已结清案件停止全部催收触达。
- 存在有效 PTP 时，可暂停普通催收触达。
- 同一客户同一渠道每日不超过策略设定次数。
- 电话仅允许在配置的业务时段内发起。
- 人工电话渠道只生成待跟进任务，不自动拨打。

策略列表支持新建、编辑、复制、启用、停用和查看执行记录。V1 使用表单和规则摘要，不建设可视化流程编排器或 A/B 实验平台。

#### 5.18.7 PTP

PTP（Promise to Pay）按借据创建，同一借据默认只允许一个有效 PTP。

字段：

- 承诺还款日期
- 承诺还款金额
- 承诺还款方式
- 客户说明
- 下次跟进时间

规则：

- 承诺金额大于 0 且不超过当前剩余应还。
- 承诺日期不得早于当天。
- 允许部分还款，一笔 PTP 可关联多笔还款记录。
- 承诺有效期内累计到账金额达到承诺金额时为“已履约”。
- 有回款但未达到承诺金额时为“部分履约”。
- 承诺日结束后仍未达到承诺金额时为“已违约”，即使此前存在部分还款。

状态：有效、今日到期、部分履约、已履约、已违约、已取消。

#### 5.18.8 人工登账

适用于客户声称已还款，但系统尚未自动识别到账的场景。

催收员提交字段：

- 借据
- 客户声称还款金额
- 还款时间
- 还款渠道
- 交易流水号
- 支付凭证
- 备注

流程：

```text
待确认 → 已确认到账
       → 已驳回
       → 已撤销
```

- 人工登账统一由财务确认。
- 驳回必须填写原因。
- 财务确认后由现有还款系统更新账单，催收系统只展示同步结果。
- 催收员不能直接修改账单余额。

#### 5.18.9 减免申请

可申请减免的费用包括利息、服务费和逾期费，默认不允许减免本金。

字段：

- 减免费用类型
- 当前费用金额
- 申请减免金额
- 减免后应还金额
- 有效截止时间
- 申请原因
- 备注或凭证

角色权限增加以下阈值：

- 减免申请金额上限
- 减免申请比例上限
- 减免审批金额上限
- 减免审批比例上限

申请和审批同时校验金额与比例，任一维度超过当前角色阈值时不得提交或审批。

流程：

```text
待审批 → 已通过 → 待客户履约 → 已生效
       → 已拒绝
       → 已撤销
                              → 已失效
```

- 审批通过仅生成限时减免方案，不立即冲减费用。
- 客户在有效期内按方案完成还款后，减免生效。
- 到期未完成方案要求时，减免失效。
- V1 不建设可视化多级审批流。

#### 5.18.10 关键操作审计

以下操作必须写入审计日志：

- 查看完整手机号
- 分案、再分配、退回待分配池
- 新增或更正催记
- 创建、修改或取消 PTP
- 提交和审核人工登账
- 提交和审批减免
- 新建、修改、启用或停用策略

**权限编码**

```
collection                           催收管理
  collection:dashboard               催收看板
    collection:dashboard:view        查看催收指标
  collection:workbench               催收工作台
    collection:workbench:view        查看本人任务及案件
    collection:workbench:call        软电话拨号
    collection:workbench:followup    记录催记
    collection:workbench:sensitive   查看完整手机号
  collection:allocation              分案管理
    collection:allocation:view       查看分案池和可分配坐席
    collection:allocation:assign     分配案件
    collection:allocation:reassign   管理员再分配
    collection:allocation:return     退回待分配池
  collection:strategy                策略配置
    collection:strategy:view         查看策略与执行记录
    collection:strategy:edit         新增、编辑、复制策略
    collection:strategy:toggle       启用或停用策略
  collection:ptp                     PTP 管理
    collection:ptp:view              查看 PTP
    collection:ptp:create            创建或更新 PTP
    collection:ptp:cancel            取消 PTP
  collection:ledger                  人工登账
    collection:ledger:create         提交人工登账
    collection:ledger:confirm        财务确认或驳回
  collection:waiver                  减免管理
    collection:waiver:create         提交减免申请
    collection:waiver:approve        审批或驳回减免
```

---

### 5.19 App 资源位管理

**角色权限：** OPERATIONS、SUPER_ADMIN

管理 App 各端资源位的展示内容和跳转配置，运营人员可实时更新 Banner、弹窗、入口图标等，无需发版。

#### Tab 1：资源位配置

| 字段 | 说明 |
|---|---|
| 位置名称 | 易读名称，如「首页顶部 Banner」 |
| 位置 Key | 唯一标识符，App 通过此 Key 拉取配置（创建后不可修改） |
| 类型 | Banner / Popup / Entry Icon / Float Button / Tab Item |
| 跳转类型 | H5（WebView）/ Native Page / External URL / No Action |
| 跳转链接 | URL 或 Native 路由 |
| 图片 URL | 图片资源地址 |
| 显示文案 | 按钮标签、Banner 标题等可选文字 |
| 排序 | 同类型资源位的展示顺序 |
| 生效时间 | 开始展示时刻（可为空，表示不限） |
| 失效时间 | 停止展示时刻（可为空，表示不限） |
| 目标用户 | All / New Users / Returning / Overdue Users |
| 状态 | Active / Inactive |
| 更新时间 | 最近修改时间 |

筛选：位置 Key / 名称关键字、类型、状态。

操作：
- **新增**：填写上述字段，Key 需唯一，创建后写入变更日志。
- **编辑**：可修改除 Key 以外的所有字段，保存后写入变更日志。
- **启用 / 禁用**：快速切换状态，写入变更日志。

#### Tab 2：变更记录

| 字段 | 说明 |
|---|---|
| 日志 ID | 系统生成 |
| 资源位 | 名称 + Key |
| 动作 | Created / Updated / Enabled / Disabled |
| 变更时间 | — |
| 操作人 | 管理员账号 |
| 变更前 | 变更前的关键字段快照 |
| 变更后 | 变更后的关键字段快照 |
| 备注 | 操作人填写的变更说明 |

筛选：位置 Key / 操作人、变更日期。

**权限编码**

```
config:app_resources                 App资源位管理
  config:app_resources:view          查看资源位列表
  config:app_resources:create        新增资源位
  config:app_resources:edit          编辑资源位
  config:app_resources:toggle        启用/禁用
  config:app_resources:log           查看变更记录
```

---

### 5.20 系统管理

**管理员账号**

- 字段：用户名、真实姓名、邮箱、手机号、角色、状态、最近登录、创建时间。
- 操作：创建、编辑、重置密码、启用/禁用、删除。
- 新密码仅展示一次。

**角色管理**

- 字段：角色编码、角色名称、描述、用户数、状态、排序、创建时间。
- 操作：创建、编辑、分配权限、查看用户、启用/禁用、删除。
- 已分配用户的角色禁止删除。

**权限管理**

- 树形展示菜单和按钮权限。
- 权限编码创建后不可修改。
- 已被角色使用的权限禁止删除。

**审计日志**

- 记录后台所有新增、编辑、删除、状态变更、审批、导出、查看敏感信息等操作。
- 字段：操作人、模块、动作、目标类型、目标 ID、IP、请求参数、结果、前后快照、时间。

---

## 6. 安全与合规

- 登录失败连续 5 次锁定 30 分钟。
- JWT 登录态，空闲超时时间 2 小时。
- 密码使用 bcrypt 或同等级安全算法存储。
- 手机号、证件号、联系人手机号、银行卡/钱包账号默认脱敏。
- 查看敏感信息、导出数据必须写入审计日志。
- 不同角色只能访问授权菜单和按钮。
- 删除均采用软删除。

---

## 7. 非功能需求

- 后台默认英文 UI，可支持中文切换。
- 业务时区：PKT（UTC+5）。
- 币种：PKR。
- 默认分页：20 条/页。
- 列表需支持筛选、分页，核心财务列表需支持导出。
- 所有金额字段保留 2 位小数。
- 枚举值由后端统一返回，前端仅做展示映射。

---

## 8. 当前原型待补事项

- 权限树需覆盖所有侧边栏菜单和关键按钮。
- PRD、权限编码、前端路由、后端接口路径需统一命名。
- 借款申请、借据、还款计划、还款、费用变更等 mock 字段必须以 `zariya_loan.sql` 为准。
- Munsalik 对账后续需接入真实邮件接收、文件解析、自动匹配、差异状态更新和审计数据接口；当前原型已完成页面设计。
- 敏感字段需补脱敏与“查看完整信息”的权限控制。

---

*文档结束*
